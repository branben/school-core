Task: Generate TypeScript diff for aquarium-reap IPC contract types

Given this contract, output ONLY the TypeScript source for src/shared/aquarium-reap.ts. Do not add any extra content.

CONTRACT:
- Export const AQUARIUM_REAP_CHANNEL = 'aquarium:reap'
- Export type AquariumReapDenyReason = 'owner-uid' | 'not-found' | 'guard-block'
- Export interface AquariumReapRequest: { repoPath: string, worktreePaths: string[] }
- Export interface AquariumReapDenied: { path: string, reason: AquariumReapDenyReason, detail?: string }
- Export interface AquariumReapResult: { reaped: string[], denied: AquariumReapDenied[], failed: { path: string, error: string }[] }

Comments to include:
// Aquarium Reap IPC contract (T7/T8 closure): the backend disposal path the
// Aquarium panel's Reap verb invokes. The renderer passes only worktree
// identities (repo path + worktree path); the server RE-DERIVES ownership and
// guard state and refuses anything it would not own.

Output format: pure TypeScript code only.